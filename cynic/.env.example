# CYNIC Environment Variables (.env.example)
# Copy to .env and customize for your environment: cp .env.example .env
# NEVER commit .env to git (add to .gitignore)
#
# Usage: docker compose --env-file .env up -d

# ── CYNIC Versioning & Versioning ────────────────────────────────────────
# CYNIC_VERSION is used to tag Docker images with semantic versions
#   - Used in docker-compose.yml: image: cynic:${CYNIC_VERSION}
#   - Allows rollback and version tracking
#   - Example: 0.2.0, 0.2.1, 1.0.0
CYNIC_VERSION=0.2.0

# ── CYNIC Core Paths (Portable across host/container) ──────────────────────
# CYNIC_HOME is the root application directory
#   - In Docker: /app (set in docker-compose.yml)
#   - On host: $(pwd)/cynic or your project root
CYNIC_HOME=/app

# CYNIC_STATE_DIR is where CYNIC stores persistent state (~/.cynic equivalent)
#   - In Docker: /app/state (managed by docker-compose volume)
#   - On host: ~/.cynic or $XDG_STATE_HOME/cynic
CYNIC_STATE_DIR=/app/state

# CYNIC_LOG_LEVEL controls logging verbosity
#   - Allowed: DEBUG, INFO, WARNING, ERROR, CRITICAL
CYNIC_LOG_LEVEL=INFO

# ── LLM & Storage Backends ───────────────────────────────────────────────
# Ollama LLM inference service
#   - In Docker: http://ollama:11434
#   - On host: http://localhost:11434
CYNIC_OLLAMA_BASE_URL=http://ollama:11434

# SurrealDB storage backend
#   - In Docker: ws://surrealdb:8000
#   - On host: ws://localhost:8001
CYNIC_SURREAL_URL=ws://surrealdb:8000
CYNIC_SURREAL_USER=root
CYNIC_SURREAL_PASS=root

# Optional: PostgreSQL database (if using instead of SurrealDB)
# CYNIC_DATABASE_URL=postgresql://user:password@postgres:5432/cynic

# ── Python Runtime ──────────────────────────────────────────────────────
# Unbuffered output for real-time logging in Docker containers
PYTHONUNBUFFERED=1

# ── Optional: Advanced Configuration ─────────────────────────────────────
# GPU acceleration (Windows host with Ollama GPU support)
#   - Set only if running Ollama on Windows GPU
# OLLAMA_GPU_URL=http://host.docker.internal:11434

# GitHub Actions self-hosted runner (for LOD 2+ deployment)
# GITHUB_REPO_URL=https://github.com/YOUR_ORG/CYNIC
# GITHUB_RUNNER_TOKEN=YOUR_RUNNER_TOKEN_HERE
