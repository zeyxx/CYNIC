# CYNIC — Agent Handoff

> Semantic context between sessions. Read at start, append at end.
> Coordination (file claims) is handled by the kernel — this carries meaning.

## [claude-fa62e82e] 2026-04-15T18:30:00Z
STATUS: Token stimulus protocol + Dog calibration + multi-agent coordination
TOUCHED:
  - cynic-kernel/src/domain/stimulus.rs (clippy fixes, module wired)
  - cynic-kernel/src/domain/mod.rs (added stimulus module)
  - cynic-kernel/domains/token-analysis.md (NEW — 6-axiom domain prompt for tokens)
  - cynic-kernel/tests/calibration_token.rs (NEW — 9-case calibration corpus)
  - docs/reference/token-43-dimensions.md (NEW — 43 dimensions × token domain)
  - docs/reference/token-calibration-baseline.md (NEW — baseline measurements)
  - docs/reference/token-stimulus-roadmap.md (NEW — 5-phase roadmap)
  - mcp-coord/ (NEW — Go MCP proxy for multi-agent coordination)
  - .gemini/settings.json (hooks + MCP coord wired)
  - .gemini/hooks/coord-claim.sh (NEW — L3 file write interception)
  - scripts/git-hooks/pre-commit (L2 coord check added)
  - AGENTS.md (rewritten for Claude/Gemini/Codex trio)
NEXT:
  - Phase 1: Deploy kernel with token-analysis domain prompt, re-measure calibration (AFTER vs baseline BEFORE)
  - Codex: wire .codex/config.toml with cynic-coord MCP
  - Gemini: create GEMINI.md instructions file
  - Structural burns: TODO #1-#4 (evaluate_progressive, judge.rs split, test infra)
BLOCKS: none
FOR_CODEX: Domain prompt domains/token-analysis.md is written but NOT deployed. The kernel needs rebuild+deploy. Don't touch stimulus.rs or domains/ — calibration baseline depends on the current state. The mcp-coord binary needs to be built on your first run (cd mcp-coord && go build -o cynic-coord .).
FOR_GEMINI: .gemini/settings.json now has hooks and MCP coord. On first launch, verify: /mcp should show cynic-coord with 7 tools. The BeforeTool hook intercepts file writes to cynic-kernel/ and auto-claims via the kernel.

## [codex-20260415-robustness] 2026-04-15T20:22:00Z
STATUS: Build infra A1 debt normalized and committed as d079edb (`fix(build): normalize rmcp build defaults`)
TOUCHED:
  - .cargo/config.toml
  - cynic-kernel/build.rs
  - .claude/rules/workflow.md
  - docs/reference/INFRA-BUILD.md
NEXT:
  - If you want one source of truth end-to-end, align any remaining shell wrappers/hooks that still export lower stack values than 64 MiB
  - Add proper Codex MCP wiring in-session later; this run used direct coord REST fallback
BLOCKS: none
FOR_OTHER_AGENT: Verified locally that `cargo build -p cynic-kernel`, `cargo build --tests -p cynic-kernel`, `cargo clippy --all -- -D warnings`, and release-path unit tests pass after the fix. Important nuance: 16 MiB is not stable enough to document as the repo default. During commit hooks, an external env override still surfaced `RUST_MIN_STACK=33554432`; build.rs now warns on that drift instead of silently accepting it.

## [codex-20260415-build-drift] 2026-04-15T20:33:00Z
STATUS: Closed the remaining repo-controlled `RUST_MIN_STACK` drift and committed it as 81472bf (`fix(build): align remaining stack overrides`)
TOUCHED:
  - scripts/git-hooks/pre-commit
  - Makefile
  - CLAUDE.md
  - cynic-kernel/src/api/mcp/build_tools.rs
NEXT:
  - Continue on the remaining fragmented workflow/handoff problems
  - Decide whether the leftover untracked scripts/projects are trash, work-in-progress, or should move under ignore rules
BLOCKS: none
FOR_OTHER_AGENT: The specific `33554432` warning source is gone. Validation after `make clean`: `cargo build --tests -p cynic-kernel` passes. One intermediate failure during validation was a stale incremental-cache rustc panic, not a stack-size regression.

## [claude-1db4c731] 2026-04-15T22:45:00Z
STATUS: TODO-ROBUSTNESS #1 & #3 COMPLETE + inter-agent protocol setup
TOUCHED:
  - cynic-kernel/src/dogs/inference.rs (phi^-1 ceiling + tag-based JSON extraction)
  - cynic-kernel/Cargo.toml (license metadata)
  - CLAUDE.md (explain 64 MiB requirement)
  - Makefile (align source_env stack)
  - cynic-kernel/src/api/mcp/build_tools.rs (subprocess env)
  - scripts/git-hooks/pre-commit (L1 pre-commit validation)
  - .gitignore (categorized artifacts + experimental dirs)
  - .codex/CODEX-PROTOCOL.md (NEW — multi-agent handoff for Codex)
COMPLETED:
  - #1: Build infra A1 debt normalized (d079edb) — RUST_MIN_STACK=64MB, RUSTFLAGS=-C debuginfo=1
  - #3: Structured commits done (8 commits) — git clean, tests 476/476 pass
  - Inter-agent coordination: .codex/CODEX-PROTOCOL.md written, cynic-coord MCP ready
NEXT:
  - #4: K15 wiring — 6 producers → acting consumers (choose 1–2 to implement)
  - #5: Peer review gates (branch protection on main, 2-approval rule for kernel/)
BLOCKS: none
FOR_CODEX: Read .codex/CODEX-PROTOCOL.md for coordination setup. Use `codex` → `/mcp` to verify cynic-coord is live (7 tools). For any cynic-kernel/ edits: claim → edit → commit → release. Handoff tracking is live via .handoff.md updates at session end.
