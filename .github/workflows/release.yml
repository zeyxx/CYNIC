name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g., 1.0.0)'
        required: true

jobs:
  validate-release:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine

    - name: Extract version
      id: version
      run: |
        if [ "${{ github.event_name }}" == "push" ]; then
          VERSION=${GITHUB_REF#refs/tags/v}
        else
          VERSION=${{ github.event.inputs.version }}
        fi
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "Version: $VERSION"

    - name: Verify version format
      run: |
        VERSION=${{ steps.version.outputs.version }}
        if [[ $VERSION =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
          echo "[OK] Valid version format: $VERSION"
        else
          echo "[FAIL] Invalid version format: $VERSION"
          echo "Expected format: X.Y.Z (e.g., 1.0.0)"
          exit 1
        fi

    - name: Run full test suite
      run: |
        pip install -e .
        pip install pytest pytest-cov
        pytest tests/ -v --tb=short || exit 1

    - name: Build distribution
      run: |
        python -m build

    - name: Check distribution
      run: |
        twine check dist/*

    - name: Create GitHub Release
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: dist/*
        body: |
          # Release ${{ steps.version.outputs.version }}

          ## Changes
          See [CHANGELOG.md](CHANGELOG.md) for detailed changes.

          ## Installation
          ```bash
          pip install CYNIC==${{ steps.version.outputs.version }}
          ```

          ## Testing
          All tests passed in this release.
        draft: false
        prerelease: false

    - name: Upload to TestPyPI
      if: github.event_name == 'workflow_dispatch'
      run: |
        twine upload --repository testpypi dist/* \
          -u __token__ \
          -p ${{ secrets.TEST_PYPI_API_TOKEN }} || true

    - name: Upload to PyPI
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
      run: |
        twine upload dist/* \
          -u __token__ \
          -p ${{ secrets.PYPI_API_TOKEN }} || true

  changelog-check:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Check CHANGELOG
      run: |
        if [ ! -f "CHANGELOG.md" ]; then
          echo "[WARNING] CHANGELOG.md not found"
          echo "See docs/CHANGELOG_TEMPLATE.md for template"
        else
          echo "[OK] CHANGELOG.md exists"
        fi

    - name: Validate version in CHANGELOG
      if: github.event_name == 'push'
      run: |
        VERSION=${GITHUB_REF#refs/tags/v}
        if grep -q "## \[$VERSION\]" CHANGELOG.md; then
          echo "[OK] Version $VERSION found in CHANGELOG"
        else
          echo "[WARN]  Version $VERSION not found in CHANGELOG"
        fi
