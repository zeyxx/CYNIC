# CYNIC Environment Configuration
# Copy to .env and fill in values for your environment
# NEVER commit .env files with real credentials

# =============================================================================
# ENVIRONMENT (development | staging | production)
# =============================================================================
NODE_ENV=development
CYNIC_ENV=development

# =============================================================================
# DATABASE (PostgreSQL)
# =============================================================================
# Development (local Docker):
# CYNIC_DATABASE_URL=postgresql://cynic:cynic@localhost:5432/cynic?sslmode=disable

# Production (get from Render dashboard - NEVER commit real credentials):
# CYNIC_DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?ssl=true
CYNIC_DATABASE_URL=

# =============================================================================
# CACHE (Redis)
# =============================================================================
# Development (local Docker):
# CYNIC_REDIS_URL=redis://localhost:6379

# Production (get from Render dashboard - NEVER commit real credentials):
# CYNIC_REDIS_URL=rediss://USER:PASSWORD@HOST:PORT
CYNIC_REDIS_URL=

# =============================================================================
# MCP SERVER
# =============================================================================
MCP_MODE=stdio
MCP_PORT=3000

# =============================================================================
# RENDER API (Infrastructure Management via MCP)
# =============================================================================
# Get from Render Dashboard > Account Settings > API Keys
RENDER_API_KEY=

# =============================================================================
# OPTIONAL: API Authentication
# =============================================================================
# CYNIC_API_KEY=your-api-key-here

# =============================================================================
# OPTIONAL: Node Identity (for P2P network)
# =============================================================================
# Leave empty to auto-generate, or provide existing keypair
# CYNIC_NODE_PRIVATE_KEY=
# CYNIC_NODE_PUBLIC_KEY=

# =============================================================================
# LLM VALIDATORS (Multi-LLM Consensus)
# =============================================================================
# Enable multi-LLM consensus for critical decisions (security, deployment)
# Comma-separated list of validators: ollama, lm-studio, airllm
# CYNIC_VALIDATORS=ollama

# Ollama Configuration (http://localhost:11434 by default)
# OLLAMA_ENDPOINT=http://localhost:11434
# OLLAMA_MODEL=llama3.2

# LM Studio Configuration (OpenAI-compatible)
# LM_STUDIO_ENDPOINT=http://localhost:1234
# LM_STUDIO_MODEL=local-model

# AirLLM Configuration (Large models via disk offloading)
# Requires: ollama pull mistral:7b-instruct-q4_0
# CYNIC_AIRLLM=true
# CYNIC_AIRLLM_MODEL=mistral:7b-instruct-q4_0

# =============================================================================
# TIER-BASED LLM ROUTING
# =============================================================================
# How CYNIC chooses which LLM to use:
#   LOCAL  → Pattern match from memory (no LLM call)
#   LIGHT  → Ollama/local models for fast, simple tasks
#   FULL   → Claude for complex reasoning
#   CONSENSUS → Multiple LLMs vote for critical decisions
#
# Consensus is triggered for: security, deployment, architecture domains
# or when request.requireConsensus = true

# =============================================================================
# SOLANA BLOCKCHAIN
# =============================================================================
# Mainnet RPC endpoint (use a dedicated provider for production)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# $asdfasdfa token mint address (mainnet, pump.fun)
BURN_TOKEN_MINT=9zB5wRarXMj86MymwLumSKA1Dx35zPqqKfcZtK1Spump

# =============================================================================
# MARKET DATA APIS (optional fallbacks)
# =============================================================================
# DexScreener: No API key required (primary source)
# Birdeye: Get from https://birdeye.so/developers
# BIRDEYE_API_KEY=

# Jupiter: No API key required for basic usage
# JUPITER_API_KEY=

# =============================================================================
# SOCIAL PERCEPTION (C4.1)
# =============================================================================
# Twitter API v2 bearer token (optional: enables real Twitter API)
# Get from https://developer.twitter.com/en/portal/dashboard
# TWITTER_API_KEY=

# Discord webhook URL (optional: enables Discord integration)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
