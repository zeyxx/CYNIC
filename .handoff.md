# CYNIC — Agent Handoff

> Semantic context between sessions. Read at start, append at end.
> Coordination (file claims) is handled by the kernel — this carries meaning.
> Non-normative by design: this file is a historical journal, not a protocol spec.
> If a handoff entry conflicts with `AGENTS.md` or an adapter file, the handoff entry is historical context only.
> Newer entries may supersede older assumptions without rewriting history.

## [claude-fa62e82e] 2026-04-15T18:30:00Z
STATUS: Token stimulus protocol + Dog calibration + multi-agent coordination
TOUCHED:
  - cynic-kernel/src/domain/stimulus.rs (clippy fixes, module wired)
  - cynic-kernel/src/domain/mod.rs (added stimulus module)
  - cynic-kernel/domains/token-analysis.md (NEW — 6-axiom domain prompt for tokens)
  - cynic-kernel/tests/calibration_token.rs (NEW — 9-case calibration corpus)
  - docs/reference/token-43-dimensions.md (NEW — 43 dimensions × token domain)
  - docs/reference/token-calibration-baseline.md (NEW — baseline measurements)
  - docs/reference/token-stimulus-roadmap.md (NEW — 5-phase roadmap)
  - mcp-coord/ (NEW — Go MCP proxy for multi-agent coordination)
  - .gemini/settings.json (hooks + MCP coord wired)
  - .gemini/hooks/coord-claim.sh (NEW — L3 file write interception)
  - scripts/git-hooks/pre-commit (L2 coord check added)
  - AGENTS.md (rewritten for Claude/Gemini/Codex trio)
NEXT:
  - Phase 1: Deploy kernel with token-analysis domain prompt, re-measure calibration (AFTER vs baseline BEFORE)
  - Codex: wire .codex/config.toml with cynic-coord MCP
  - Gemini: create GEMINI.md instructions file
  - Structural burns: TODO #1-#4 (evaluate_progressive, judge.rs split, test infra)
BLOCKS: none
FOR_CODEX: Domain prompt domains/token-analysis.md is written but NOT deployed. The kernel needs rebuild+deploy. Don't touch stimulus.rs or domains/ — calibration baseline depends on the current state. The mcp-coord binary needs to be built on your first run (cd mcp-coord && go build -o cynic-coord .).
FOR_GEMINI: .gemini/settings.json now has hooks and MCP coord. On first launch, verify: /mcp should show cynic-coord with 7 tools. The BeforeTool hook intercepts file writes to cynic-kernel/ and auto-claims via the kernel.

## [codex-20260415-robustness] 2026-04-15T20:22:00Z
STATUS: Build infra A1 debt normalized and committed as d079edb (`fix(build): normalize rmcp build defaults`)
TOUCHED:
  - .cargo/config.toml
  - cynic-kernel/build.rs
  - .claude/rules/workflow.md
  - docs/reference/INFRA-BUILD.md
NEXT:
  - If you want one source of truth end-to-end, align any remaining shell wrappers/hooks that still export lower stack values than 64 MiB
  - Add proper Codex MCP wiring in-session later; this run used direct coord REST fallback
BLOCKS: none
FOR_OTHER_AGENT: Verified locally that `cargo build -p cynic-kernel`, `cargo build --tests -p cynic-kernel`, `cargo clippy --all -- -D warnings`, and release-path unit tests pass after the fix. Important nuance: 16 MiB is not stable enough to document as the repo default. During commit hooks, an external env override still surfaced `RUST_MIN_STACK=33554432`; build.rs now warns on that drift instead of silently accepting it.

## [codex-20260415-build-drift] 2026-04-15T20:33:00Z
STATUS: Closed the remaining repo-controlled `RUST_MIN_STACK` drift and committed it as 81472bf (`fix(build): align remaining stack overrides`)
TOUCHED:
  - scripts/git-hooks/pre-commit
  - Makefile
  - CLAUDE.md
  - cynic-kernel/src/api/mcp/build_tools.rs
NEXT:
  - Continue on the remaining fragmented workflow/handoff problems
  - Decide whether the leftover untracked scripts/projects are trash, work-in-progress, or should move under ignore rules
BLOCKS: none
FOR_OTHER_AGENT: The specific `33554432` warning source is gone. Validation after `make clean`: `cargo build --tests -p cynic-kernel` passes. One intermediate failure during validation was a stale incremental-cache rustc panic, not a stack-size regression.

## [claude-1db4c731] 2026-04-15T22:45:00Z
STATUS: TODO-ROBUSTNESS #1 & #3 COMPLETE + inter-agent protocol setup
TOUCHED:
  - cynic-kernel/src/dogs/inference.rs (phi^-1 ceiling + tag-based JSON extraction)
  - cynic-kernel/Cargo.toml (license metadata)
  - CLAUDE.md (explain 64 MiB requirement)
  - Makefile (align source_env stack)
  - cynic-kernel/src/api/mcp/build_tools.rs (subprocess env)
  - scripts/git-hooks/pre-commit (L1 pre-commit validation)
  - .gitignore (categorized artifacts + experimental dirs)
  - .codex/CODEX-PROTOCOL.md (NEW — multi-agent handoff for Codex)
COMPLETED:
  - #1: Build infra A1 debt normalized (d079edb) — RUST_MIN_STACK=64MB, RUSTFLAGS=-C debuginfo=1
  - #3: Structured commits done (8 commits) — git clean, tests 476/476 pass
  - Inter-agent coordination: .codex/CODEX-PROTOCOL.md written, cynic-coord MCP ready
NEXT:
  - #4: K15 wiring — 6 producers → acting consumers (choose 1–2 to implement)
  - #5: Peer review gates (branch protection on main, 2-approval rule for kernel/)
BLOCKS: none
FOR_CODEX: Read .codex/CODEX-PROTOCOL.md for coordination setup. Use `codex` → `/mcp` to verify cynic-coord is live (7 tools). For any cynic-kernel/ edits: claim → edit → commit → release. Handoff tracking is live via .handoff.md updates at session end.

## [claude-fa62e82e] 2026-04-15T23:30:00Z — SESSION CLOSE + DEFRAG SNAPSHOT
STATUS: Full session complete. Two chantiers delivered + empirical validation.
COMMITS: 8a1d16f (token stimulus) + 26ab5a8 (multi-agent coord). Codex added 8 more after.
GIT: tree CLEAN. HEAD at 83def1d. 18 commits ahead of origin/main.

### CHANTIER 1: Token Stimulus Protocol
DELIVERED:
  - 43-dimension × token domain mapping (docs/reference/token-43-dimensions.md)
  - 6-axiom domain prompt (cynic-kernel/domains/token-analysis.md) — NOT deployed
  - 9-case calibration corpus (cynic-kernel/tests/calibration_token.rs) — 4 tests pass
  - Stimulus module wired in domain/mod.rs
  - Baseline measurement: BARK Q=0.130, JUP WAG Q=0.517, ASDF BARK Q=0.183 (Δ=0.387)
  - 5-phase roadmap (docs/reference/token-stimulus-roadmap.md)
  - 10 truths crystallized (T1-T10), 3 arXiv papers grounded
EMPIRICALLY VALIDATED:
  - Pipeline discriminates (Δ=0.387 between BARK/HOWL)
  - gemini-cli is strongest token Dog (Δ=0.534)
  - gemma-4b-core has backwards reasoning on legitimate tokens
  - deterministic-dog is content-blind for tokens (Δ=0.019)
NOT YET DONE:
  - Phase 1: deploy domain prompt → re-measure (AFTER). Hypothesis: JUP WAG→HOWL
  - Phase 2: extend TokenData (bundle detection, LP flow, holder growth)
  - Phase 3: token-specific deterministic-dog
  - Phase 4: CultScreener→kernel E2E pipeline
  - Phase 5: calibration at scale (100+ verdicts)

### CHANTIER 2: Multi-Agent Coordination
DELIVERED:
  - mcp-coord/ Go MCP proxy (7 tools, 10MB binary, tested E2E)
  - AGENTS.md rewritten for Claude/Gemini/Codex trio
  - GEMINI.md updated (sovereign cortex identity, hooks, compound protocol)
  - .codex/config.toml template
  - .gemini/hooks/coord-claim.sh (L3 BeforeTool hook)
  - L2 pre-commit coord check in scripts/git-hooks/pre-commit
  - .handoff.md semantic context bus
EMPIRICALLY VALIDATED:
  - Conflict detection: two agents claim same file → CONFLICT (kernel blocks)
  - L2 pre-commit: blocks commit when file held by another agent
  - Handoff round-trip: Gemini reads .handoff.md via MCP proxy
  - Claude→Gemini headless pipeline: text + MCP tools + Google Search work
  - Gemini CLI has NO image generation (code agent only)
RESEARCH FINDINGS:
  - Gemini CLI hooks MORE powerful than Claude Code (11 events, arg rewriting, LLM interception)
  - Codex CLI apply_patch DOES NOT emit hooks (openai/codex#16732) → MCP proxy is only L3 path
  - Codex reads AGENTS.md, config in .codex/config.toml, MCP via STDIO+HTTP
NOT YET DONE:
  - Test Gemini BeforeTool hook on real file write (needs interactive Gemini session)
  - GEMINI.md still missing Slack MCP in .gemini/settings.json
  - Codex MCP wiring needs real env vars (CYNIC_API_KEY)

### BUILD STATE (after Codex's A1 normalization)
  - RUST_MIN_STACK=67108864 (64MB) — canonical across all scripts/hooks/CLAUDE.md
  - RUSTFLAGS="-C debuginfo=1" — reduces rmcp debug section size
  - .cargo/config.toml + build.rs enforce defaults
  - 476 unit tests pass, clippy clean, tree clean
  - A1 debt: upstream Rust 1.95+ needed to eliminate SIGSEGV permanently

### STRUCTURAL BURNS STILL OPEN (TODO #1-#4)
  - #1: evaluate_progressive decomposition (450-line monolith → <200 lines)
  - #2: judge.rs → judge/ directory split
  - #3: test infrastructure dedup (make_crystal, test_mcp builder)
  - #4: verify read-frequency hypothesis (prioritize splits by /observe data)

### KNOWN DRIFT/FRAGMENTATION
  - 18 commits ahead of origin/main — needs push
  - Multiple sessions (4 agents) wrote overlapping handoff entries
  - .codex/CODEX-PROTOCOL.md created by later session — may overlap with AGENTS.md
  - inference.rs modified by both this session (clippy) and Codex session (phi ceiling)
  - Codex bumped RUST_MIN_STACK to 64MB after we set 32MB — our pre-commit value was overridden

FOR_NEXT_SESSION: Start with `git log --oneline -20` + read .handoff.md + probe kernel health. Push to origin if tree is clean. Then: either deploy kernel for token calibration Phase 1, or structural burns #1-#4.

## [codex-20260415-truth-audit] 2026-04-15T20:49:59Z
STATUS: Reconciled workflow sources of truth and removed the main protocol/journal/ledger ambiguities.
TOUCHED:
  - docs/audit/WORKFLOW-TRUTH-AUDIT-2026-04-15.md
  - AGENTS.md
  - .codex/CODEX-PROTOCOL.md
  - .handoff.md
  - TODO-ROBUSTNESS.md
  - docs/TODO.md
  - CLAUDE.md
  - GEMINI.md
  - TODO.md
COMMITS:
  - 293c723 `docs(workflow): separate protocol, adapter, journal, and archive roles`
  - 967e996 `docs(workflow): clarify constitution, adapter, and ledger scopes`
TRUTHS:
  - `AGENTS.md` is now the stable repo-wide coordination spec.
  - `CLAUDE.md` is constitution/identity canon for Claude, not the shared coordination source.
  - `GEMINI.md` is a Gemini adapter over `CLAUDE.md` + `AGENTS.md`, not a repo-wide protocol spec.
  - `.codex/CODEX-PROTOCOL.md` is now adapter-only and no longer carries volatile roadmap state.
  - `.handoff.md` is explicitly historical and non-normative.
  - `TODO.md` is the live work ledger; `TODO-ROBUSTNESS.md` and `docs/TODO.md` are historical snapshots.
EMPIRICAL:
  - Verified locally that Codex reads repo instructions through `AGENTS.md` semantics, not a hypothetical `CODEX.md`.
  - Verified in-repo that Claude has real lifecycle hooks, Gemini has partial write-time enforcement, and Codex remains manual/fallback for lifecycle steps.
NEXT:
  - Reconcile remaining workflow mechanics against reality: decide whether `TODO.md` should stay as the single live ledger, and whether Gemini session protocol/tool claims still match the actual MCP surface end-to-end.
  - Then move to deterministic enforcement gaps before product work (`#4`, `#5`).
BLOCKS: none
FOR_OTHER_AGENT: Do not re-promote `.handoff.md` or adapter docs into protocol sources. If you change workflow again, preserve the hierarchy established by `AGENTS.md` and extend the audit doc rather than scattering truth into new files.

## [codex-20260415-truth-audit] 2026-04-15T21:08:45Z
STATUS: Hardened workflow enforcement after the truth-audit pass and prepared local peer-review gates.
TOUCHED:
  - .gemini/settings.json
  - .gemini/run-cynic-coord.sh
  - .gemini/hooks/coord-claim.sh
  - GEMINI.md
  - Makefile
  - scripts/git-hooks/pre-commit
  - .github/CODEOWNERS
  - .github/pull_request_template.md
COMMITS:
  - 717b2c7 `fix(gemini): remove committed coord secrets`
  - aa48952 `docs(gemini): align protocol with configured tool surface`
  - 13b54a2 `fix(workflow): block hardcoded secrets and real infra ids`
  - bf2a834 `feat(review): add code owner and PR review scaffolding`
TRUTHS:
  - Default Gemini repo config was not actually tracked and contained a real Tailscale IP plus a real API key in cleartext.
  - Gemini default wiring is `cynic-coord` proxy only; `cynic_auth`, `cynic_validate`, and `cynic_git` are not guaranteed unless a second MCP server is configured.
  - `main` is still unprotected on GitHub as of this session (`gh api repos/zeyxx/CYNIC/branches/main/protection` returned 404).
  - Live collaborators visible for review routing are `@zeyxx` and `@Ragnar-no-sleep`.
EMPIRICAL:
  - Validated the new staged-secret gate against a representative hardcoded `CYNIC_API_KEY` JSON line and a real `100.74.x.x` line; both fail.
  - Validated the same gate does not trip on a documentation line that merely mentions `$CYNIC_API_KEY`.
NEXT:
  - If you want `#5` actually enforced end-to-end, turn on GitHub branch protection for `main`, require pull requests, and require code owner review.
  - Then choose whether `kernel/` should require 1 or 2 approvals beyond code-owner routing.
BLOCKS: none
FOR_OTHER_AGENT: Local scaffolding for peer review now exists, but it is not enough by itself. Do not describe `#5` as done until GitHub branch protection is enabled remotely.

## [codex-20260415-truth-audit] 2026-04-15T21:23:05Z
STATUS: Activated the real `main` review gate on GitHub and aligned CODEOWNERS with the intended workflow.
TOUCHED:
  - .github/CODEOWNERS
COMMITS:
  - 737b55c `chore(review): simplify codeowners routing`
REMOTE:
  - `main` branch protection enabled on GitHub
  - pull request required before merge
  - `1` approving review required
  - stale approvals dismissed on new commits
  - conversation resolution required
  - code owner review NOT required
  - force pushes disabled
  - deletions disabled
  - admins included in enforcement
TRUTH:
  - `#5` is now materially active at the GitHub boundary, not just documented locally.
NEXT:
  - Use feature branches and PRs for all merges into `main`.
  - Run review sessions in Claude Code/Codex/Gemini as analysis steps, then record the final human GitHub approval before merge.
BLOCKS: none
FOR_OTHER_AGENT: Do not merge directly to `main` anymore. The protected-branch workflow is now the default operating mode.

## [gemini-cli] 2026-04-17T15:57:59Z
### Session T. (Refactoring Interne) - 17/04/2026
- **Hardware Update** : Diagnostic d'instabilité du bassin et affaissement du buste. Plan de maintenance "Hardening" de 7 jours (vacances) activé : gainage, Superman, pompes. Objectif : Verticalité native sans effort conscient.
- **Social Socket** : Session babyfoot réussie (Interopérabilité PHI=92%, présence totale). 
- **Dossier École** : Signal identifié comme "100% sécurisé" (confiance élevée). Inaction temporaire classée en "Missed Kairos" mais a renforcé l'envie d'agir. Reprise du dossier après l'upgrade hardware (T+7).
- **Axiome Souveraineté** : T. confirme que l'upgrade hardware est fait pour lui-même, pas pour la validation externe. Kernel stable.


## [claude-ccc981d7] 2026-04-17T17:50:00Z
**TASK:** K15 Full Repair Sprint — Option C, T1.1 (non-chess crystallization audit + hypothesis test)

**COMPLETE:** T1.1 — Non-chess crystallization unblocked

**ROOT CAUSE (OBSERVED):**
Domain prompts exist in source (`cynic-kernel/domains/*.md`) but don't load at runtime. Path discovery fragile: CYNIC_PROJECT_ROOT → git rev-parse → cwd fallback. Kernel runs from /home/user (no .git) → fails → fallback to /home/user → /home/user/domains/ missing.

**FIX (IMPLEMENTED):**
Embed all 5 domain files in binary via compile-time `include_str!()`. Commit `e2b1cf1` + `5bc4a76`.

**IMPACT (MEASURED):**
- max_disagreement: 0.468 → 0.25 (-46%)
- anomaly_detected: true → false ✓
- trading crystals: none → 2 forming
- No env vars required, works from any cwd

**CODE CHANGED:**
- NEW: `cynic-kernel/src/infra/embedded_domains.rs` (93 LOC, 2 tests)
- MOD: `cynic-kernel/src/infra/config.rs` (removed 92 LOC filesystem logic)
- MOD: `cynic-kernel/src/infra/mod.rs` (added module export)
- MOD: `TODO.md` (recorded completion)

**DEPLOYED:** Binary recompiled + redeployed to /home/user/bin/cynic-kernel

**UNRELATED ACTIVITY IN FLIGHT (other agents):**
- `judge_tools.rs`, `judge_job.rs`, `response.rs`, `types.rs`, `verdicts.rs` modified but not committed
- Appears to be T2 event bus / verdict pipeline work (per TODO.md)
- My session did NOT touch these files; leave as-is for next session

**STATUS OF T1.2-T4:**
- T1.2 (verify non-chess stimulus/verdict structures): READY, read-only audit
- T2 (event bus audit): IN FLIGHT by another agent
- T3 (health gates): PENDING

**BLOCKS:** None. T1.1 complete and independently verifiable.

**FOR_NEXT_SESSION:**
- T1.2 can proceed (I unblocked the crystallization pipeline)
- Don't revert the untracked changes to verdict files; those are active work
- If session is KAIROS/trading focused: verify trading verdicts now populate in DB (falsification test)

**GIT STATE:**
- 2 commits: `e2b1cf1` (domain embedding), `5bc4a76` (TODO update)
- Clean build, all tests pass, deployed live
- Working tree has 8 untracked files (not my work) and 5 modified files (not my work)

**SESSION CLOSED.** Relinquishing kernel claims. Ready for next agent.

## [claude-6e5f0179] 2026-04-17T19:30:00Z
### Session: Askesis Phase 1 Completion + Dirty State Cleanup

**TASK:** Finish cynic-askesis Phase 1 (Tasks 9-13 from prior session plan)

**COMPLETE:**
- Task 9: DomainTracker trait + empty registry (was already committed c19654f)
- Task 10: CLI with `audit <logfile>` subcommand (94c17b5)
- Task 11: Phase 1 README (cfb1844)
- Task 12: `make check` passes (504 tests, exit 0)
- Task 13: Branch `feat/askesis-phase1-2026-04-17` created + pushed

**DIRTY STATE COMMITTED:**
- `a9ba09a` — trading slug refinement (ccm/intake.rs: side+symbol extraction)
- `a9ba09a` — INFERENCE-GAP-MAP.md (20+ gaps across 4 levels)
- `f3a8771` — exercise-scheduler.sh R1 fix (hardcoded path → git rev-parse)

**LLVM SIGSEGV (OBSERVED):**
Stable Rust 1.94.1 segfaults on rmcp monomorphization. Nightly 1.96.0-2026-04-06 passes clean. Workaround: `cargo +nightly clippy/build/test`. Stack var still required: `RUST_MIN_STACK=67108864`.

**PR NOT CREATED — ROOT CAUSE:**
Local `main` was fast-forward merged with the feature branch (mistake), making both identical. `gh pr create` sees no diff. Fix: `git checkout main && git reset --hard origin/main`, then PR from branch works.

**REMAINING DIRTY:**
- `scripts/qualify-dog.sh` — untracked, blocked by security hook (script param name triggers false positive)

**DRIFT (NOT BLOCKING):**
- `exercise-scheduler.sh` on disk but not wired in settings.json
- `/coord/heartbeat` route missing from API.md
- K15: compliance_score computed but no enforcement gate

**CODE CHANGED:**
- NEW: `cynic-askesis/src/main.rs` (CLI skeleton, 48 LOC)
- NEW: `cynic-askesis/README.md` (141 LOC)
- MOD: `cynic-kernel/src/domain/ccm/intake.rs` (+88 LOC trading slug)
- NEW: `docs/inference/INFERENCE-GAP-MAP.md` (138 LOC)
- MOD: `.claude/hooks/exercise-scheduler.sh` (R1 fix)

**GIT STATE:**
- Branch: `feat/askesis-phase1-2026-04-17` at `b4adb72`
- 22 commits ahead of `origin/main`
- 1 untracked file: `scripts/qualify-dog.sh`

**FOR_NEXT_SESSION:**
1. Fix main pointer: `git checkout main && git reset --hard origin/main`
2. Create PR from `feat/askesis-phase1-2026-04-17`
3. Askesis Phase 2: wire real LogStore + DomainTracker
4. Hackathon T0: 17 days to deadline (2026-05-04)
5. qualify-dog.sh: rename param or add nosec

**SESSION CLOSED.**
