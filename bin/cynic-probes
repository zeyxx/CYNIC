#!/usr/bin/env python
"""
CYNIC Probes CLI — Self-Improvement Proposal Management

Entry point for the cynic-probes command-line tool.
Manages self-improvement proposals generated by the SelfProber (Priority 10 Task 3).

Usage:
    cynic-probes list [PENDING|APPLIED|DISMISSED|all]
    cynic-probes show <probe_id>
    cynic-probes approve <probe_id>
    cynic-probes dismiss <probe_id>
    cynic-probes audit [--limit 50]
    cynic-probes help
"""

import sys
import os

# Ensure CYNIC package is importable
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

from cynic.interfaces.cli.probes_commands import main

if __name__ == "__main__":
    main()
