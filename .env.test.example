# CYNIC Test Environment Variables
#
# Copy to .env.test for running tests locally
# CI uses GitHub Actions secrets instead
#
# "φ distrusts φ" - κυνικός

# ==============================================================================
# Test Environment
# ==============================================================================
NODE_ENV=test

# ==============================================================================
# Database (Optional for tests)
# ==============================================================================
# Tests run with in-memory storage by default
# Set these only if you want to test with real PostgreSQL

# Option 1: Full URL (replace <password> with actual password)
# CYNIC_DATABASE_URL=postgresql://test:<password>@localhost:5432/cynic_test?sslmode=disable

# Option 2: Component vars (for Docker Compose)
# CYNIC_DB_HOST=localhost
# CYNIC_DB_PORT=5432
# CYNIC_DB_USER=test
# CYNIC_DB_PASSWORD=test
# CYNIC_DB_NAME=cynic_test

# ==============================================================================
# Redis (Optional for tests)
# ==============================================================================
# CYNIC_REDIS_URL=redis://localhost:6379

# ==============================================================================
# MCP Configuration
# ==============================================================================
MCP_MODE=stdio
PORT=3001

# ==============================================================================
# Test-specific Settings
# ==============================================================================
# Disable external network calls in tests
CYNIC_OFFLINE_MODE=true

# Faster timeouts for tests
CYNIC_TEST_TIMEOUT=5000
