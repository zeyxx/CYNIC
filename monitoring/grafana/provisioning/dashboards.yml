apiVersion: 1

providers:
  - name: 'CYNIC Dashboards'
    orgId: 1
    folder: 'CYNIC'
    folderUid: 'cynic'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
