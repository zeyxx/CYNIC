# Windows UTF-8 Encoding Fix
# Add this to your system environment variables or use it with:
#   set /p < .env.windows  (Command Prompt)
#   Get-Content .env.windows (PowerShell)

PYTHONIOENCODING=utf-8
PYTHONUNBUFFERED=1
