{
  "$schema": "../../schemas/dog-patterns.json",
  "dog": "janitor",
  "sefira": "Yesod",
  "version": "1.0.0",
  "description": "L1 complexity patterns for code simplification",

  "complexityThresholds": {
    "description": "φ-aligned thresholds for complexity detection",
    "fileLines": {
      "warning": 500,
      "critical": 1000,
      "reason": "Files over 500 lines are hard to maintain"
    },
    "functionLines": {
      "warning": 50,
      "critical": 100,
      "reason": "Functions over 50 lines should be split"
    },
    "cyclomaticComplexity": {
      "warning": 10,
      "critical": 20,
      "reason": "High cyclomatic complexity = hard to test"
    },
    "dependencies": {
      "warning": 13,
      "critical": 21,
      "reason": "Too many dependencies = coupling smell (Fib thresholds)"
    },
    "nestingDepth": {
      "warning": 4,
      "critical": 6,
      "reason": "Deep nesting is hard to read"
    }
  },

  "codeSmells": {
    "description": "Patterns that indicate code needs cleanup",
    "patterns": [
      {
        "name": "god_object",
        "description": "Class/module doing too much",
        "indicators": ["methods > 20", "lines > 1000", "responsibilities > 3"]
      },
      {
        "name": "dead_code",
        "description": "Unreachable or unused code",
        "indicators": ["no imports", "no calls", "commented out blocks"]
      },
      {
        "name": "duplicate_code",
        "description": "Copy-pasted logic",
        "indicators": ["similarity > 80%", "same logic different names"]
      },
      {
        "name": "long_parameter_list",
        "description": "Functions with too many params",
        "indicators": ["params > 5", "consider object parameter"]
      },
      {
        "name": "primitive_obsession",
        "description": "Using primitives instead of small objects",
        "indicators": ["many string params", "coordinate pairs as separate args"]
      }
    ]
  },

  "simplificationSuggestions": {
    "description": "Automatic suggestions for detected issues",
    "bySmell": {
      "god_object": "Consider splitting into smaller, focused modules",
      "dead_code": "Consider removing with BURN analyzer verification",
      "duplicate_code": "Extract to shared utility function",
      "long_parameter_list": "Group related params into an options object",
      "primitive_obsession": "Create a value object or type"
    }
  },

  "learnedPatterns": [],

  "metadata": {
    "createdAt": "2026-02-02T00:00:00Z",
    "updatedAt": "2026-02-02T00:00:00Z",
    "learningEnabled": true,
    "lastLearningUpdate": null
  }
}
